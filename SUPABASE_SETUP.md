# ربط الموقع بـ Supabase — دخول المالك بكود عبر الإيميل

الكود جاهز في app.js و index.html. الباقي إعدادات في لوحة Supabase (5 دقائق) وتعبيان قيمتين في الكود.

## 1) إنشاء المشروع
1. supabase.com → New project (اختر أي اسم وباسورد للـ database).
2. بعد الإنشاء: Settings → API → انسخ:
   - **Project URL**
   - **anon / public key** (أو publishable key الجديد)

## 2) تعبية القيم في الكود
في ملف `app.js`، بداية قسم "owner mode" (حوالي السطر 366):

```js
const SUPABASE_URL="YOUR_SUPABASE_URL";        // ← الصق Project URL هنا
const SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"; // ← الصق anon/public key هنا
```

## 3) إنشاء حساب المالك (الإيميل المسموح له فقط)
Authentication → Users → **Add user** → **Create new user**:
- Email: إيميل يوسف (مثلاً `yousef.harthi@alnahda.edu.om`، أو أي إيميل تحددونه للمالك)
- ✅ فعّلي **Auto Confirm User**
- ما تحتاجين تعبين باسورد — الدخول بالكود بس.

⚠️ **مهم:** الكود بالموقع مضبوط على `shouldCreateUser:false` — يعني أي إيميل غير موجود بهذي القائمة **ما يوصله كود أبداً**، حتى لو أحد جرّب. فقط الإيميل اللي تسوينه هنا كـ user يقدر يدخل كمالك. لو تبين تضيفين مالك ثاني بعدين، كرري هذي الخطوة بإيميله.

## 4) تفعيل ظهور الكود في الإيميل (خطوة سهل تفوت)
Authentication → Email Templates → **Magic Link** → لازم يكون فيه `{{ .Token }}` في نص الرسالة (القالب الافتراضي أحياناً يعرض بس رابط بدون الكود الرقمي). لو ناقص، أضيفي بالنص شي زي:

```
كود الدخول: {{ .Token }}
```

بدون هذا، الإيميل بيوصل لكن بدون الكود المكوّن من 6 أرقام اللي يحتاجه المستخدم يكتبه بالموقع.

## 5) (اختياري) لو الإيميلات تصير بطيئة أو محدودة
Supabase الافتراضي يرسل عدد محدود من الإيميلات بالساعة على الخطة المجانية. لو صار تأخير، فعّلي SMTP خاص من Authentication → Settings → SMTP Settings (مثلاً Resend أو أي مزوّد بريد).

## كيف تصير التجربة بعد الإعداد
- المالك يضغط 🔒 بأعلى الموقع → يكتب إيميله → يستلم كود من 6 أرقام بالإيميل → يكتب الكود → يفتح وضع التعديل.
- الجلسة تفضل مفتوحة لين يضغط 🔓 (يقفل) صراحةً، أو تنتهي صلاحيتها من طرف Supabase.
- أي زائر ثاني يجرب إيميل مو مسجّل، ما يستلم أي كود — بدون رسالة تفصح إذا الإيميل موجود أو لا (حماية إضافية).
