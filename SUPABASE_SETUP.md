# ربط الموقع بـ Supabase — دخول المالك برابط عبر الإيميل

القيم (URL + Key) معبّاة بالفعل في app.js. باقي بس إعدادات بلوحة Supabase.

## 1) الإيميل المسموح له (المالك)
Authentication → Users → **Add user** → **Create new user**:
- Email: إيميل المالك (مثلاً `fffaissal.96@gmail.com`)
- ✅ فعّلي **Auto Confirm User**
- بدون باسورد — الدخول برابط بالإيميل بس.

⚠️ **مهم:** الكود مضبوط على `shouldCreateUser:false` — أي إيميل غير موجود بهذي القائمة **ما يوصله شي أبداً**. فقط الإيميل اللي تسوينه هنا user يقدر يدخل كمالك. لو تبين مالك ثاني، كرري نفس الخطوة بإيميله.

## 2) رابط الموقع الصحيح (خطوة ضرورية)
Authentication → **URL Configuration**:
- **Site URL**: حطي رابط الموقع الفعلي بالضبط، مثلاً:
  `https://faisal-portfolio-sable.vercel.app`
- **Redirect URLs**: أضيفي نفس الرابط (وممكن نضيف `https://faisal-portfolio-sable.vercel.app/**` احتياط).

بدون هذي الخطوة، رابط "Sign in" اللي يوصل بالإيميل يرجّع لصفحة localhost وما يشتغل.

## لماذا رابط بدل كود مكتوب؟
القالب الافتراضي لإيميلات Supabase (بدون إعداد SMTP خاص) ما يقدر يعرض كود رقمي — يعرض بس رابط "Sign in". إعداد SMTP خاص (Resend وغيره) شغل إضافي مو ضروري، فالكود الحين يستخدم نفس الرابط الجاهز مباشرة — أبسط وأسرع بدون خطوات زيادة، ونفس مستوى الأمان (رابط صالح لاستخدام واحد وينتهي بسرعة).

## كيف تصير التجربة
- المالك يضغط 🔒 بأعلى الموقع → يكتب إيميله → يوصله إيميل فيه رابط "Sign in" → يضغطه → يرجع للموقع تلقائياً ووضع التعديل مفعّل.
- الجلسة تفضل مفتوحة لين يضغط 🔓 (قفل) صراحةً، أو تنتهي صلاحيتها من طرف Supabase.
- أي إيميل ثاني غير مسجّل، ما يستلم أي رسالة — بدون أي إشارة إذا الإيميل موجود أو لا.

## (اختياري) لو الإيميلات صارت بطيئة
Supabase الافتراضي يرسل عدد محدود من الإيميلات بالساعة على الخطة المجانية. لو صار تأخير ملحوظ، فعّلي SMTP خاص من Authentication → Settings → SMTP Settings.
